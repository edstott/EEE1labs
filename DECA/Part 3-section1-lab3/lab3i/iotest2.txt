LDR R3, [#-1] // load COUNT
LDR R0, [#-2] // load IRQ status
AND R0, #2 // test IRQ1
JEQ -3
MOV R0, #2
STR R0, [#-1] // reset IRQ1
MOV R0, #2
STR R0, [#-1] // stop reset
ADD R2, #1 // increment software counter
JMP -9

