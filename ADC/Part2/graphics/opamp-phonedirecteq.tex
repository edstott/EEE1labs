\documentclass{standalone}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage[europeanresistors, europeanports]{circuitikz}
\usetikzlibrary{calc,positioning}

\begin{document}

		\begin{circuitikz}[american voltages]
			\small
			\draw node[ground] {} to[short,sV,l=$V_\text{source}$,n=sig] ++(0,3)
			to[R,l=$R_\text{source}$,a=1k$\Omega$,n=rsource] ++(2,0)
			to[C,l=$C_\text{source}$,a=3.3μF,n=csource, l2 halign=c, l2 valign=b] ++(2,0)
			-- ++(1.0,0) coordinate(br1)
			-- ++(0.5,0) coordinate(br)
			-- ++(1.5,0) 
			to[R,n=rphone,l2=$R_\text{load}$ and $64\Omega$, l2 halign=c] ++(0,-1.5) node{}
			to[C,n=cphone,l2=$C_\text{load}$ and 50μF, l2 halign=c] ++(0,-1.5)  node[ground] {};
			;

			\draw (br1) to[short,-o] ++(0,5mm) node[above=1mm]{$V_\text{in}$};
			\draw[dashed]
				(sig.west) ++ (-20mm,-5mm)
				rectangle
				($(csource.east) + (10mm,10mm)$)
				;

			\draw[dashed]
				(cphone.west) ++ (-10mm,-10mm)
				rectangle
				($(rphone.east) + (20mm,23mm)$)
				;

			\node[above] at ($(sig.north) + (10mm,25mm)$) {signal source};
			\node[above] at ($(rphone.north) + (5mm,18mm)$) {earphones};

		\end{circuitikz}

\end{document}